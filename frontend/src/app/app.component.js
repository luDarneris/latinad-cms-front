import angular from "angular";

angular.module("latinadCmsApp").component("appRoot", {
  template: `
    <main>
      <h1 class="text-3xl font-bold text-blue-600">LatinAd CMS</h1>
    </main>
  `
});
